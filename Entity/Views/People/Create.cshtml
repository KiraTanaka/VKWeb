@{
    ViewBag.Title = "Добавление нового пользователя";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Person</legend>

        <div class="editor-label">
           Ссылка на страницу нового пользователя <br />
        </div>
        <div class="editor-field">
            <input type="text" name="userUrl" />
        </div>

        @*<div class="editor-label">
            @Html.LabelFor(model => model.LastName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.LastName)
            @Html.ValidationMessageFor(model => model.LastName)
        </div>*@
        <div class="editor-label">
           Добавить друзей @Html.CheckBox("addFriends", false) <br />
           (если это ссылка на страницу)
        </div>
        <br />
        <div class="editor-label">
           Добавить подписчиков сообщества  @Html.CheckBox("addMembersOfGroup", false) <br />
           (если это ссылка на сообщество)
        </div>
        <p>
            <input type="submit" value="Добавить" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Вернуться к списку", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
